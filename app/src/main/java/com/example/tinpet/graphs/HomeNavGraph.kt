package com.example.tinpet.graphs
/*
import androidx.compose.runtime.Composable
import androidx.navigation.NavGraphBuilder
import androidx.navigation.NavHostController
import androidx.navigation.compose.NavHost
import androidx.navigation.compose.composable
import com.example.tinpet.AppScreens
import com.example.tinpet.screens.HomeScreen
import com.example.tinpet.screens.PlacesScreen
import com.example.tinpet.screens.ProfileScreen

@Composable
fun HomeNavGraph(navController: NavHostController) {
    NavHost(
        navController = navController,
        route = Graph.HOME,
        startDestination = AppScreens.Home.route
    ) {
        composable(route = AppScreens.Home.route) {
            HomeScreen()
        }
        composable(route = AppScreens.Profile.route) {
            ProfileScreen(
                onClick ={
                    navController.popBackStack()
                    navController.navigate(Graph.SETTINGS)
                }
            )
        }
        composable(route = AppScreens.Places.route) {
            PlacesScreen()
        }
    }
}*/
